import API from "./stationCollection";
import stationList from "./stationList"

const stationEditor = {
  editForm(stationId, stationObject) {
    console.log("button is clicked")
  }
}


export default stationEditor