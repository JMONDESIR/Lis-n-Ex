const mediaPlayer = {
  media() {
    const header = document.querySelector("header")
    const mp3_player = document.createElement("div")
    const audio_box = document.createElement("div")
    const analyser = document.createElement("canvas")

    header.appendChild(mp3_player)
    mp3_Player.appendChild(audio_box)
    mp3_Player.appendChild(analyser)
  }

}

export default mediaPlayer
